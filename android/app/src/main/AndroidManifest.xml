<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <!-- audio_service holds a PARTIAL_WAKE_LOCK while playback is active so
         audio keeps running with the screen off. Without this permission,
         enterPlayingState() throws SecurityException before reaching
         startForeground(), which Android then reports as an ANR. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Declares that the app runs on Android Automotive OS (AAOS) head
         units in addition to phones. required="false" keeps the same APK
         installable on regular phones, where Google Play Services'
         Android Auto host (`com.google.android.projection.gearhead`)
         binds our MediaBrowserService over the phone-to-car link
         instead. -->
    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="false" />
    <application
        android:label="JRiver Remote"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Android Auto (phone-based, via GMS gearhead host app):
             registers JRR as a `media` category app. The head unit reads
             this descriptor on bind, then talks to the MediaBrowserService
             declared further down. -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <!-- Android Automotive OS (AAOS, native on the head unit — no
             phone). AAOS uses a different meta-data name to discover the
             same descriptor; declaring both keeps a single APK working on
             both phone-AA and AAOS. -->
        <meta-data
            android:name="com.android.automotive"
            android:resource="@xml/automotive_app_desc" />

        <!-- Android Auto: small monochrome notification icon. Reuses the
             alpha-mask drawable added in Phase 2 for the system media
             notification — Auto enforces the same alpha-mask rule, and
             pointing at the multicolor mipmap/ic_launcher would crash on
             some head units. -->
        <meta-data
            android:name="com.google.android.gms.car.notification.SmallIcon"
            android:resource="@drawable/ic_audio_service_notification" />

        <!-- audio_service: foreground media playback service. -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- audio_service: media-button (Bluetooth, headset) receiver. -->
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- Exposes cached album artwork to Android Auto via content:// URIs.
             AA runs in a different process and cannot read file:// URIs
             into app-private storage, so MediaItem.artUri is emitted as
             content://com.jrr.jrr_f.fileprovider/root/<absolute-path>.
             grantUriPermissions=true lets the MediaBrowserService binder
             pass per-URI read access to the AA client. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
